package helpers;

/**
 * Stores the possible values for the "status" attribute of a "Ride" object.
 */
public enum RideStatus {
	simulated,
	trashed,
	unconfirmed,
	cancelled,
	confirmed,
	ongoing,
	completed;
}
